pipeline {
    agent any
    enviroment {
        DEFAULT_DIR = "/var/betterme/script"
    }
    stages {
        stage("Prepare Update") {
            steps {
                dir("${DEFAULT_DIR}") {
                    echo "[INFO] Prepare Stage"
                    sh "./prepare_update.sh"
                    echo "Use Node v17"
                }
            }
        }
        stage("Run Frontend") {
            steps {
                
                echo "[INFO] Run Frontend"

            }
        }
    }
}